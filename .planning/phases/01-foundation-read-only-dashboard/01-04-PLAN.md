---
phase: 01-foundation-read-only-dashboard
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bifrost/mcp/tools.py
  - tests/test_tools.py
autonomous: true
gap_closure: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, HUMAN-01, HUMAN-02]

must_haves:
  truths:
    - "MCP agents calling bifrost_list_agents can identify which agent is the human operator"
    - "MCP agents calling bifrost_whoami on the human operator see an is_human indicator"
  artifacts:
    - path: "src/bifrost/mcp/tools.py"
      provides: "is_human marker in handle_list_agents and handle_whoami text output"
      contains: "is_human"
    - path: "tests/test_tools.py"
      provides: "Tests verifying is_human appears in tool output"
      contains: "is_human"
  key_links:
    - from: "src/bifrost/mcp/tools.py"
      to: "src/bifrost/store/models.py"
      via: "Agent.is_human field read in handle_list_agents and handle_whoami"
      pattern: "a\\.is_human|agent\\.is_human"
---

<objective>
Close the HUMAN-02 gap: MCP tool text output (handle_list_agents, handle_whoami) must include an is_human indicator so agents querying via MCP can programmatically distinguish the human operator from regular agents.

Purpose: The UI portion of HUMAN-02 is complete (human-card class, Human badge, separate section). The API portion is missing -- MCP tool responses do not expose the is_human flag.
Output: Updated tools.py with is_human markers in text output, corresponding tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-read-only-dashboard/01-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add is_human indicator to handle_list_agents and handle_whoami</name>
  <files>src/bifrost/mcp/tools.py, tests/test_tools.py</files>
  <read_first>
    - src/bifrost/mcp/tools.py (full file -- understand current output format)
    - tests/test_tools.py (TestHandleWhoami and TestHandleListAgents classes)
    - src/bifrost/store/models.py (Agent dataclass, confirm is_human field exists at line ~129)
  </read_first>
  <behavior>
    - Test: handle_whoami on human operator agent (is_human=True) includes "human: yes" or "[human]" in output
    - Test: handle_whoami on regular agent (is_human=False) does NOT include human marker
    - Test: handle_list_agents output marks human operator with "[human]" tag in the agent line
    - Test: handle_list_agents output for regular agents does NOT include "[human]" tag
  </behavior>
  <action>
    1. In tests/test_tools.py, add a fixture that creates a human operator agent (is_human=True) using the existing store/hubs pattern. Add tests to TestHandleWhoami and TestHandleListAgents verifying the is_human indicator appears for human agents and does NOT appear for regular agents.

    2. Run tests -- confirm new tests FAIL (RED).

    3. In src/bifrost/mcp/tools.py, modify handle_whoami (around line 100-109):
       - After the Status line, add: if agent.is_human: lines.append("Human: yes")

    4. In src/bifrost/mcp/tools.py, modify handle_list_agents (around line 123-129):
       - Change the line format to include a [human] tag when a.is_human is True:
         tag = " [human]" if a.is_human else ""
         lines.append(f"- {a.name} [{a.status}]{tag}{desc}")

    5. Run tests -- confirm all pass (GREEN).

    Do NOT change any other tool handler. Do NOT modify models.py or db.py (is_human field already exists and works).
  </action>
  <verify>
    <automated>cd /home/marc/dev/bifrost && .venv/bin/pytest tests/test_tools.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_tools.py contains test that creates agent with is_human=True and verifies "Human: yes" appears in handle_whoami output
    - tests/test_tools.py contains test that verifies "[human]" appears in handle_list_agents output for human agents
    - tests/test_tools.py contains test that verifies regular agents do NOT have "[human]" in handle_list_agents output
    - handle_whoami in tools.py contains line checking agent.is_human
    - handle_list_agents in tools.py contains line checking a.is_human
    - Full test suite passes: .venv/bin/pytest tests/ -x -q shows 0 failures
  </acceptance_criteria>
  <done>MCP tool responses include is_human indicator. Human operator is programmatically distinguishable via both bifrost_whoami and bifrost_list_agents. All tests pass.</done>
</task>

</tasks>

<verification>
1. `.venv/bin/pytest tests/test_tools.py -x -q` -- all tool tests pass including new is_human tests
2. `.venv/bin/pytest tests/ -x -q` -- full suite passes with no regressions
3. `grep -n "is_human" src/bifrost/mcp/tools.py` -- confirms is_human checks in both handle_whoami and handle_list_agents
</verification>

<success_criteria>
- HUMAN-02 fully satisfied: human operator distinguishable in BOTH UI (already done) AND API responses (this plan)
- No regressions in existing test suite
- Minimal change footprint: only tools.py and test_tools.py modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-read-only-dashboard/01-04-SUMMARY.md`
</output>
